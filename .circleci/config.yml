version: 2
jobs:
  build:
    machine:
      image: circleci/classic:edge
    steps:
      - checkout
      - run:
          name: docker-compose up
          command: docker-compose up -d
      - run:
          name: wait to run
          command: sleep 10
      - run:
          name: app test
          command: docker-compose run app python ../test/*test.py
      - run:
          name: bertserver test
          command: docker-compose run bertserver python ../test/*test.py
      - run:
          name: mlserver test
          command: docker-compose run mlserver python ../test/*test.py
      - run:
          name: scraping batch test
          command: docker-compose run scraping_batch python ../test/*test.py
      - run:
          name: docker-compose down
          command: docker-compose down

  deploy:
    machine:
      image: circleci/classic:edge
    steps:
      - checkout
      - add_ssh_keys:
        -run: ssh ${USER_NAME}@${HOST_NAME} 'git pull && docker-compose restart'

workflows:
  version: 2
  build_and_deploy:
    jobs:
      - build
      - deploy:
        requires:
          - build
        filters:
          branches:
            only: master
