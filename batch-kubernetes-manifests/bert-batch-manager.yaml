apiVersion: v1
kind: Service
metadata:
  name: bert-batch-manager
spec:
  selector:
    app: bert-batch-manager
  ports:
  - protocol: "TCP"
    port: 3035
    targetPort: 3035
    nodePort: 30005
  type: NodePort

---
apiVersion: batch/v1
kind: Job
metadata:
  name: bert-batch-manager
spec:
  completions: 1
  parallelism: 1
  backoffLimit: 0
  template:
    metadata:
      labels:
        app: bert-batch-manager
    spec:
      containers:
      - name: bert-batch-manager
        image: opeco17/batch-manager:latest
        imagePullPolicy: Never
        ports:
        - containerPort: 3035
        env:
        - name: COMPLETIONS
          value: "3"
        - name: UWSGI_PORT
          value: "3035"
        - name: TZ
          valueFrom:
            configMapKeyRef:
                name: general-config
                key: tz
      restartPolicy: Never